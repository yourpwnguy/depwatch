version: "2"

linters:
  default: standard
  enable:
    - misspell
    - bodyclose
    - unconvert
    - goconst
    - gocritic

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

run:
  timeout: 5m
  tests: true
